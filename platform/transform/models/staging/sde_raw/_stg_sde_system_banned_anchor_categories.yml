version: 2

models:
  - name: sde_system_banned_anchor_categories
    description: "System-specific anchoring restrictions for entire item categories. Prevents deployment of certain structure categories in designated systems, typically used for high-security space restrictions or special system rules like trade hubs."
    columns:
      - name: system_id
        description: "Solar system with anchoring restrictions. References stg.sde_systems (required)"
      - name: category_id
        description: "Prohibited item category. References stg.sde_categories (required)"
